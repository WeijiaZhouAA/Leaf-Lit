# Local PostgreSQL connection. Copy this file to .env and adjust if needed.
# The bundled database started with `npm run db:start` uses these defaults.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/leaflit?schema=public"

# Secret used to sign session cookies. Use a long random string.
AUTH_SECRET="replace-with-a-long-random-string"
